# Build output
dist/

# Dependencies
node_modules

# Test / tooling artifacts
output/playwright/
.playwright-cli/
coverage/

# Logs & debug
*.log
npm-debug.log*

# Local environment
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store

# Local agent scratch
.claude/
/.worktrees/

# Bun standalone build artifacts
src/server/assets/*.tar.gz
/libexec/desk-standalone
/libexec/moor
# Retired runtime symlink; stale copies linger in old worktrees and must
# never be swept into a merge resolution again (that is how b87aba0 shipped
# a machine-local absolute symlink).
/libexec/atch
/probe-server
/probe-*.mjs

# Internal working notes never enter the public tree
docs/superpowers/
